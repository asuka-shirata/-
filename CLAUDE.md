# ClaudeCode勉強会 プロジェクト

## プロジェクト概要

Claude Code の学習・実践用プロジェクト。複数のサブプロジェクトを含む。

### ファイル構成

| ファイル / フォルダ | 内容 |
|---|---|
| `catchup-dashboard.html` | キャッチアップ用ダッシュボード |
| `praise.html` | 褒め言葉生成ページ |
| `serve.py` | ローカル開発サーバー |
| `デイリーメンタルトレーニング/` | メンタルトレーニング用 HTML アプリ |
| `フォルダ整理/` | ファイル自動仕分けスクリプト |
| `売上分析/` | CSV データ分析・レポート生成スクリプト |

## Git 運用ルール

### 基本方針

**コードを変更するたびに、必ず GitHub へプッシュすること。**

### 手順

1. **変更後は即コミット & プッシュ**
   ```bash
   git add <変更ファイル>
   git commit -m "変更内容の説明"
   git push origin main
   ```

2. **コミットメッセージの書き方**
   - 日本語・英語どちらでも可
   - 何を・なぜ変えたかを簡潔に記述する
   - 例: `catchup-dashboard: チャート表示のバグを修正`

3. **まとめてコミットしない**
   - 1つの変更 = 1コミットを基本とする
   - 複数ファイルを同時に変更した場合も、論理的なまとまりでコミットを分ける

4. **ブランチ運用**
   - 小規模な修正は `main` ブランチに直接コミットしてよい
   - 大きな機能追加や実験的な変更は feature ブランチを作成する
   ```bash
   git checkout -b feature/新機能名
   ```

### Claude が作業するときのルール

- ファイルを編集したら、作業完了前に必ず `git push` まで実行する
- `git push` が失敗した場合はユーザーに報告し、原因を調査する
- 破壊的な git 操作（`reset --hard`、`push --force` など）は必ずユーザーの確認を取る

## 開発環境

- Python 3 系
- ローカルサーバー起動: `python serve.py`
